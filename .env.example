# Frontend Environment Variables
# Copy this file to .env.local and modify the values as needed

# API URL - Base URL for the backend API
# Development: http://localhost:3000/api
# Production (same host): /api
# Production (different host): https://api.your-domain.com/api
VITE_API_URL=/api
